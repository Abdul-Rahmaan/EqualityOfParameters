# this program verify the equality of two different values given the parameters a and b.
when Both the value and type of the parameters are equal or same then it return true otherwise it returns false.
